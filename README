This is a simple test for the websocket functionality in atmosphere.

The web application works fine streaming and using long-polling but fails to work with websockets.
The cause seems to be that the webapp is run underneath a context path "/wike". Moving it to "/" (root) it works fine.

(c) 2011 Matthias L. Jugel http://twimpact.com/
